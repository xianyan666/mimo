# AI 协作决策记录 — task-manager Bug 修复

> 每条记录包含五个字段。最关键的是「人工判断」——不是"AI说的对"，而是你采纳/修改/拒绝了AI的什么建议、为什么。

---

## 第 1 条

| 字段 | 内容 |
|------|------|
| **目的** | 修复 task_manager.py 中的两个 bug，使所有测试通过 |
| **输入** | 项目路径 `s2/`，要求阅读文档并定位 bug。运行 `python test.py` 发现 3 个测试失败（对应两个 bug） |
| **建议** | 1. Bug 1：在 `add_task` 中将 `priority` 转换为 `int`，失败时默认为 0；2. Bug 2：在 `add_task` 中校验 `title` 是否为空，为空则抛出 `ValueError` |
| **人工判断** | 采纳了修复建议，并要求执行修复 |
| **验证** | 运行 `python test.py`，7 个测试全部通过 |

---

## 第 2 条

| 字段 | 内容 |
|------|------|
| **目的** | 补充测试用例，验证修复后的功能和其他边界情况 |
| **输入** | 用户要求补充测试用例，并记录到 ai-log.md |
| **建议** | 创建 `test_extra.py`，包含 6 个测试用例：1. 数字字符串 priority 转换；2. 负数 priority 排序；3. title 前后空格 trim；4. add_task 返回列表引用；5. 多个相同优先级任务；6. filter_by_priority 边界值 |
| **人工判断** | 采纳了补充测试用例的建议，并执行测试 |
| **验证** | 运行 `python test_extra.py`，7 个补充测试全部通过 |