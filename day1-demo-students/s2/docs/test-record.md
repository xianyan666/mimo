# 测试记录 — task-manager

> 三栏格式：输入 / 预期输出 / 实际输出。不用跑代码就能验证。

---

## 测试 1：正常输入

| 输入 | 预期输出 | 实际输出 |
|------|---------|---------|
| `add_task([], '学习 RAG', 1)` | 列表含 1 个任务，title='学习 RAG', priority=1 | |
| 依次添加 `('低优先',1)`, `('高优先',5)`, `('中优先',3)` | 顺序：高优先→中优先→低优先 | |

---

## 测试 2：边界输入（Bug 2：空 title）

| 输入 | 预期输出 | 实际输出 |
|------|---------|---------|
| `add_task([], '', 1)` | [补全] | [补全] |
| `add_task([], '   ', 1)` | [补全] | [补全] |

---

## 测试 3：异常输入（Bug 1：字符串 priority）

| 输入 | 预期输出 | 实际输出 |
|------|---------|---------|
| `add_task([], '重要任务', 'high')` 后接 `add_task([], '普通任务', 'low')` | [补全] | [补全] |

---

## 补全说明

- 先运行 `python3 test.py`，观察哪些测试失败
- 定位Bug根因后，填写上表中 `[补全]` 的预期输出和实际输出
- 修复Bug后重新跑测试，更新实际输出列为修复后的结果
