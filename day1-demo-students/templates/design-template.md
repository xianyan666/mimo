# Design: [项目名称]

## 1. 数据流总览

```
[资料来源] → [切片] → [检索] → [Prompt拼接] → [生成] → [输出]
```

## 2. 切片策略

- **切片单位**：[每个XX为一个chunk]
- **chunk 结构**：`{ id: '[唯一ID]', text: '完整内容' }`
- **chunk 数量**：[N个]

## 3. 检索逻辑

```
检索函数签名：[function(params) → return_type]

步骤：
1. [分词/向量化]
2. [匹配计算]
3. [排序]
4. [过滤]
5. [返回 TopK]
```

## 4. Prompt 模板

```
系统指令：[只基于资料回答]

上下文：[检索到的 TopK chunk]

用户问题：[question]

输出要求：[格式/来源引用/拒答]
```

## 5. 拒答规则

触发条件：
1. [条件1]
2. [条件2]

拒答回复：[固定文案]

## 6. 接口适配

| 接口 | 输入 | 输出 |
|------|------|------|
| [函数名] | [参数] | [返回值] |
