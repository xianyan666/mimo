# FAISS最小测试总结

## 测试环境
- Python 3.13.5
- faiss-cpu 1.14.3
- Windows 10/11

## 测试内容

### 1. Flat索引（暴力搜索）
- **L2距离（欧氏距离）**：精确搜索，无误差
- **内积**：计算向量内积作为相似度
- **余弦相似度**：通过归一化向量后计算内积

### 2. IVF索引（倒排文件索引）
- 需要训练阶段（聚类）
- 通过nprobe参数控制搜索范围
- 搜索速度快于Flat索引

### 3. HNSW索引（分层可导航小世界图）
- 不需要显式训练
- 构建时间较长，但搜索速度快
- 内存占用较大

### 4. PQ索引（乘积量化）
- 需要训练阶段
- 内存占用小
- 有精度损失

### 5. IVF+PQ索引
- 结合IVF和PQ的优点
- 适合大规模数据集

### 6. 持久化测试
- 成功保存和加载索引
- 验证搜索结果一致性

## 性能比较

| 索引类型 | 训练耗时 | 添加耗时 | 搜索耗时 |
|---------|---------|---------|---------|
| Flat (暴力搜索) | 0.0000s | 0.0066s | 0.1277s |
| IVF-Flat (nlist=100) | 0.0000s | 0.0285s | 0.0015s |
| HNSW (M=32) | 0.0000s | 0.7757s | 0.0017s |
| PQ (m=8, nbits=8) | 0.0000s | 0.1010s | 0.0787s |
| IVF-PQ (nlist=100, m=8) | 0.0000s | 0.1360s | 0.0025s |

## 算法原理

### Flat (暴力搜索)
- **原理**：直接计算查询向量与所有数据库向量的距离
- **优点**：精确搜索，无误差
- **缺点**：速度慢，时间复杂度O(n*d)
- **适用**：小数据集或需要精确结果的场景

### IVF (倒排文件索引)
- **原理**：先将向量空间划分为nlist个聚类，搜索时只检查最近的nprobe个聚类
- **优点**：大幅减少搜索范围，速度提升
- **缺点**：需要训练，可能漏掉一些近邻
- **参数**：nlist（聚类数量）、nprobe（搜索时检查的聚类数量）

### HNSW (分层可导航小世界图)
- **原理**：构建多层图结构，每层都是一个小世界图，搜索时从顶层开始逐层下降
- **优点**：搜索速度快，精度高
- **缺点**：内存占用大，构建时间长
- **参数**：M（每个节点的连接数）、efConstruction（构建时的搜索范围）、efSearch（搜索时的搜索范围）

### PQ (乘积量化)
- **原理**：将高维向量分成m个子空间，每个子空间用nbits位的码本表示
- **优点**：内存占用小，适合大规模数据
- **缺点**：有精度损失，需要训练
- **参数**：m（子量化器数量）、nbits（每个子量化器的位数）

### IVF+PQ (倒排文件+乘积量化)
- **原理**：结合IVF和PQ，先用IVF缩小搜索范围，再用PQ进行近似距离计算
- **优点**：兼顾速度和内存，适合大规模数据
- **缺点**：需要训练，精度有损失
- **参数**：nlist、m、nbits

## 学习建议

1. **从Flat索引开始**：理解基本概念和API
2. **调整IVF的nprobe参数**：观察精度和速度的变化
3. **比较不同索引**：测试内存占用和搜索速度
4. **实际数据集测试**：在真实数据上验证性能

## 运行测试

```bash
# 激活conda环境
conda activate faiss_test

# 运行测试脚本
python faiss_test.py
```

## 文件说明

- `faiss_test.py`：完整的测试脚本
- `flat_index.faiss`：Flat索引的持久化文件
- `ivf_index.faiss`：IVF索引的持久化文件

## 下一步学习

1. **GPU加速**：使用faiss-gpu版本
2. **更多索引类型**：BinaryFlat、ScalarQuantizer等
3. **参数调优**：使用自动调优API
4. **实际应用**：图像检索、文本相似度等